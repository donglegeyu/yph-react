# 2026-04-07 工作记录

## 菜单管理页面 - 系统菜单权限修复

### 问题
系统菜单（menuType === 'system'）被前端代码禁用了状态切换、编辑、删除功能，导致用户无法操作。

### 修复
移除了 MenuManagement.vue 中对系统菜单的操作限制：
1. ✅ 状态切换：移除 `:disabled="record.menuType === 'system'"`
2. ✅ 编辑功能：移除编辑按钮和表单中的 disabled 限制
3. ✅ 删除功能：允许删除系统菜单
4. ⚠️ 添加子级：仍限制一级系统菜单不能添加子级

### 相关文件
- 前端：`src/views/settings/MenuManagement.vue`
- 后端：`material-system-server/` (NavMenuController/Service - 后端无特殊限制)

### 待验证
- 系统菜单状态切换后，导航栏是否能正确隐藏/显示菜单（之前已修复 visibleFirstMenus 和 currentSecondMenus 的过滤逻辑）

## 2026-04-07 晚间工作记录

### 用户个人排序 API
- 新建后端 API：`GET/POST /api/user-menu-sort`
- 新建表：`user_menu_sort`（用户ID + 菜单key + 排序值）
- 前端 `fetchUserMenuSort()` 同时获取菜单管理API列表 + 用户排序API
- 前端 `setFirstMenusSort()` 保存排序时调用 POST API
- Docker 镜像已重新构建：`material-backend-test`

### 三级菜单 path 修复
- **问题**：三级菜单的 path 存的是二级菜单路径（如 `/purchase-base-data`），导致路由不匹配白屏
- **修复**：直接更新数据库 nav_menu 表中三级菜单的 path 为正确的路由路径
- 受影响菜单：采购最小起订量、采购最低允收期、采购退货地址维护、价格查询、销售扣点比例查询

### "建设中"页面统一
- 创建通用组件：`src/views/common/BuildingView.vue`（使用首页占位图样式）
- 批量替换 20 个 purchase 页面的 `<a-result>` 为 `<BuildingView />`
- `/building` 路由改为使用 `HomeView.vue`

### 更多抽屉数据同步
- `fetchBusinessMenusFromAPI()` 改为同步现有菜单信息（label/icon/status）+ 追加新菜单
- `fetchUserMenuSort()` 同时获取：1) 菜单管理API完整列表 2) 用户排序API → 应用排序

### 菜单管理列表优化
- 添加排序字段（sortOrder）到列和编辑表单
- 系统菜单删除按钮禁用
- 添加 scrollX 和 fixed: left/right 列
- 操作列宽度调整为 186px，路由列宽度 200px

### 文档更新
- DESIGN_STANDARD.md 添加 BuildingView 占位页面说明
- 创建 scripts/update-docs.js 自动更新文档脚本（待完善）
