# 2026-04-04 工作记录

## 登录页优化
- **滑块验证文字**：`请向右滑动验证` → `按住滑块，拖动到最右边`，颜色改为 `rgba(0,0,0,0.88)`（同滑块图标）
- **锁图标替换**：本地 sprite 中 `#lock` 图标视觉偏小，替换为 IconPark 官方 CDN 版本（锁身 30→36px，整体更饱满）
- **验证码登录 tab**：隐藏"记住登录"和"重置密码"行
- **获取验证码按钮**：尺寸更新为 132×52px，背景 `var(--ant-color-bg-layout)`
- **form-item margin-bottom**：保持 16px 不变
- **验证码输入框与登录按钮间距**：增加 16px 占位块
- **登录区域定位逻辑**：首次进入居中显示，onMounted 时计算表单卡片距顶部偏移量存 localStorage；切换验证码登录时用缓存值固定 paddingTop + justify-content: flex-start，切回密码登录恢复居中。解决切换 tab 时登录框跳动问题。

## 验证通过状态样式修复（21:20-21:25）
- **颜色**：文字和勾图标从黑色改为绿色 `#52C41A`（硬编码替代 CSS 变量确保生效）
- **布局**：勾图标从左移到右，"验证通过"文字在前居中（DOM 顺序调整 + space-between）
- 两处滑块模板都已同步修改
