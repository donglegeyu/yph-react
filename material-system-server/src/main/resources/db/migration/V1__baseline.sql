-- ============================================================
-- V1__baseline.sql
-- Flyway 基线版本（标记当前数据库状态）
-- 此脚本为空，仅用于建立 Flyway 版本链的起点
-- 之前的所有数据库结构由 init/ 目录的 SQL 负责
-- 后续增量变更请创建 V2__xxx.sql, V3__xxx.sql ...
-- ============================================================

-- 故意为空：baseline 之后从这里开始增量
SELECT 1;
